# 版本演进记录

## v1：桌面基础版

第一版完成了可用的 Windows 桌面助手框架：

- 小型窗口和始终置顶
- 常用回复的新增、编辑、删除、搜索和使用次数记录
- `Alt+1～9` 全局快捷回复
- 本地规则式礼貌润色
- TXT、Markdown、Word 和 PDF 文档导入与检索
- SQLite 保存话术和文档索引
- 支持通过 PyInstaller 打包为 EXE

这一版验证了“打开一个小工具，在工作时快速粘贴回复和查询资料”的核心思路。

## v2：界面与跨软件润色

在 v1 基础上重点改善外观和外部软件使用体验：

- 界面调整为莫兰迪粉、米白和鼠尾草绿色
- 新增 `Ctrl+Alt+R`：选中其他软件中的文字后原地礼貌润色
- `run.bat` 和打包脚本优先复用现有 `.venv`
- 补充从 v1 升级且保留环境和个人数据的说明

由于中文输入法通常不会向普通键盘监听程序稳定暴露最终提交的汉字，本版采用“选中文字后主动润色”，避免监控输入造成误替换。

## v3：浏览器扩展

第三版把桌面软件和网页输入场景拆分处理：

- 新增 Edge/Chrome Manifest V3 扩展
- 扩展能够识别普通网页中的 `input`、`textarea` 和部分可编辑区域
- 在用户允许的域名中自动执行礼貌替换
- 支持网页内 `Alt+1～9` 快捷话术
- 新增扩展弹窗、设置页和本地测试网页
- 网站按域名授权，默认不会在任意网站自动运行

浏览器扩展比桌面键盘监听更了解网页输入框，但仍可能受封闭 iframe、Canvas 编辑器和网站自身快捷键影响。

## v4：Linux/HPC 知识库

第四版面向值班过程中常见的超算命令查询需求：

- 新增 Linux/HPC 命令知识库
- 支持“怎么看节点”“作业为什么排队”“显存”等中文关键词
- 同时支持 `squeue`、`tail`、`grep` 等英文或命令关键词
- 命令面板支持方向键选择、Enter 复制和 Esc 关闭
- Linux/HPC 搜索默认快捷键为 `Ctrl+Shift+K`
- 网页话术由 `Alt+数字` 改为 `Ctrl+Shift+数字`，降低浏览器快捷键冲突

## v5：快捷键全面可配置

第五版重点解决微信、浏览器和其他软件占用快捷键的问题：

- 桌面助手新增独立的“快捷键”设置页面
- 每个桌面功能都可选择修饰键与主键
- 保存后自动重新注册并显示成功或冲突状态
- 防止两个助手功能设置为同一组合
- 桌面话术默认改为 `Ctrl+Alt+1～9`，减少与微信 `Alt+数字` 的冲突
- 网页话术的修饰键组合可在扩展设置页修改
- Linux/HPC 搜索快捷键继续交由浏览器扩展快捷键系统管理
- README 补充桌面端、网页端和 Linux/HPC 快捷键的完整修改方法

## v6：纯桌面 Mini Bar

第六版不再依赖浏览器扩展，重点提高微信和 Windows 软件中的可靠性：

- 主窗口改为输入法候选栏风格的窄横条
- 每条回复独立设置完整快捷键，回复数量不再限制为 9 条
- 热键触发瞬间记录前台窗口，避免助手窗口抢走微信焦点
- 等待 Ctrl、Alt、Shift 等修饰键完全松开后再发送 `Ctrl+V`
- 支持按照分类、内容或关键词搜索并粘贴首条结果
- 自动迁移 v1-v5 的话术和快捷键编号
- 保留选中文字后一键礼貌替换
- 明确说明普通程序无法稳定监听中文输入法最终提交的汉字

主要在微信及其他 Windows 软件中使用时，v6 是当前推荐版本；需要浏览器内 Linux/HPC 命令面板时仍可继续使用 v5。

## v7：系统托盘与桌面命令库

第七版继续采用纯 Windows 桌面路线：

- 隐藏后常驻 Windows 右下角系统托盘
- 托盘菜单支持显示、管理、隐藏和退出
- 新增 `启动助手.vbs`，启动时不显示 CMD 黑框
- 在桌面管理页加入 Linux、Conda 和 Slurm 命令库
- 内置 113 条常用命令、Slurm 参数和作业脚本模板
- 支持中文口语、英文、命令及关键词搜索
- 用户可以自行新增、编辑和删除命令
- 自定义命令继续保存在本地 SQLite 数据库中
- 支持通过可自定义的全局快捷键直接打开独立命令搜索框
- 删除主横条中不必要的话术搜索输入框
- 调整系统托盘消息循环，提高 `pythonw` 启动时的显示稳定性

v7 是当前推荐的纯桌面版本。
